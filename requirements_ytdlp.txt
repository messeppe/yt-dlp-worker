# yt-dlp media path (2026-07). Pin loosely; Coolify rebuild picks latest on deploy.
yt-dlp>=2026.7.4
boto3>=1.34
psycopg2-binary>=2.9
# Optional PO Token plugin (ENABLE_BGUTIL=1). Not a Google account.
# Must match Dockerfile BGUTIL_VERSION / git tag (server + plugin same release).
bgutil-ytdlp-pot-provider==1.3.1
